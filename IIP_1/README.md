For the juptyer notebooks, this repo uses [nbstripout](https://github.com/kynan/nbstripout). Make sure it's installed by running

```bash
pip install --upgrade nbstripout
nbstripout --install --attributes .gitattributes
```
